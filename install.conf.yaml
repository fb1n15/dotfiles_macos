- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: 
    ~/.vimrc: 
    ~/.zshrc: 
    ~/.gitconfig: 

- create:
    - ~/Work

- shell:
  -
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
