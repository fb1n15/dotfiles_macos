- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: 
    ~/.vimrc: 
    ~/.zsh:
    ~/.zshrc: 
    ~/.zshenv: 
    ~/.gitconfig: 
    ~/.config/bat:
      create: true
      path: config/bat
    ~/.config/karabiner:
      create: true
      path: config/karabiner


- create:
    - ~/Work

- shell:
  - command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
  - command: ./setup_zsh.zsh
    stdout: true
    stderr: true
