- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: 
    ~/.vimrc: 
    ~/.zshrc: 
    ~/.gitconfig: 
    ~/.config:
      create: true
      path: config

- create:
    - ~/Work

- shell:
  -
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
